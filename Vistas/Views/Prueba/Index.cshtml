<!DOCTYPE html>
<html>
<head>
    <title>@ViewData["Title"]</title>
    <style>
        /* Estilos personalizados */
        body {
            margin: 0; /* Elimina el margen predeterminado del body */
            padding: 0; /* Elimina el relleno predeterminado del body */
            overflow-x: hidden; /* Oculta la barra de desplazamiento horizontal */
        }

        .container {
            max-width: 960px; /* Ancho máximo del contenido principal */
            margin: 0 auto; /* Centra el contenido principal horizontalmente */
            padding: 20px; /* Espaciado interior para separar el contenido */
            box-sizing: border-box; /* Incluye el padding en el cálculo del ancho */
        }

        .footer-container {
            width: 100%; /* Ancho completo */
            position: fixed; /* Fijar el contenedor del footer */
            left: 0; /* Alinear a la izquierda */
        }

        .footer-top {
            top: 50px; /* Alinear arriba y bajar 50px */
            background-color: #0073e6; /* Azul similar al de DHL */
            color: white; /* Texto en color blanco para contrastar */
            padding: 20px; /* Espaciado interior para separar el contenido */
            text-align: center; /* Centrar el texto */
        }

        .footer-bottom {
            bottom: 0; /* Alinear abajo */
            background-color: #0073e6; /* Azul similar al de DHL */
            color: white; /* Texto en color blanco para contrastar */
            padding: 20px; /* Espaciado interior para separar el contenido */
            text-align: center; /* Centrar el texto */
        }
    </style>


    <style>
        /* Estilos personalizados para el footer superior */
        .footer-top {
            background-color: #0073e6; /* Color azul */
            color: white; /* Texto en color blanco */
        }
    </style>

    <style>
        /* Estilos personalizados para el footer inferior */
        .footer-bottom {
            background-color: #2b3035; /* Color gris oscuro */
            color: #aab4bf; /* Texto en color gris claro */
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Index</h1>

        <!-- Formulario -->
        <form>
            <label for="nombre">Nombre:</label><br>
            <input type="text" id="nombre" name="nombre"><br>
            <label for="email">Email:</label><br>
            <input type="email" id="email" name="email"><br><br>
            <input type="submit" value="Enviar">
        </form>
    </div>

    <!-- Contenedor del Footer superior -->
    <div class="footer-container footer-top">
        <!-- Footer superior -->
        <div class="footer">
            Este es el footer superior con un color azul similar al de DHL.
        </div>
    </div>

  

        </body>
